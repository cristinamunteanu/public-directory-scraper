# Local Postgres example.
# Edit this if your username, password, host, port, or database name is different.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/public_directory_scraper

# Default ETL runtime options used when CLI flags are omitted.
DEFAULT_PAGES=2
DEFAULT_TIMEOUT=10
DEFAULT_RETRIES=1
DEFAULT_DELAY=1
